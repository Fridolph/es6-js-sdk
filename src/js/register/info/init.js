import '../../common/polyfill'
import render from './render'
import {bindEvent} from './event'

export const regInfo = (opts = {}) => {
  const defaultOpts = {

  }

  const options = Object.assign(defaultOpts, opts)
}